# branching_model — release-candidate strategy

A reference repository illustrating a **release-candidate (RC)** workflow,
where releases are stabilized through `*-RCn` tags before the final tag.

![Release-candidate branching model](rc.svg)
